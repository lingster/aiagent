---
name: example-skill
description: Example skill demonstrating YAML frontmatter metadata
---

# Example Skill

This is an example skill that demonstrates how to use YAML frontmatter for metadata.

## Usage

This skill shows how to structure a skill with:
- YAML frontmatter for metadata
- Custom skill name
- Clear documentation structure

## Example Script

You can run the example script with:

```bash
python hello.py
```

## Features

- Custom naming via YAML frontmatter
- Metadata-driven configuration
- Clean separation of metadata and content
