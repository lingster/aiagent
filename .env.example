# AI Agent Configuration Example
# Copy this file to .env and update with your values

# ==============================================================================
# Data Directory Configuration
# ==============================================================================
# Path to the directory that will be mounted in the Docker container
# This is where your code and files will be stored
DATA_PATH=/path/to/your/code

# ==============================================================================
# Backend API Configuration
# ==============================================================================
# Port for the backend REST API (runs inside Docker)
# This is the port where shell commands are executed
MCP_PORT=8181

# ==============================================================================
# Remote MCP Server Configuration
# ==============================================================================
# Port for the remote MCP server (Streamable HTTP transport)
# This is the port that MCP clients will connect to
MCP_REMOTE_PORT=8888

# API Key for remote MCP authentication
# Generate a secure random string for production
# Leave empty for development mode (authentication disabled)
# Example: openssl rand -base64 32
MCP_API_KEY=your-secure-api-key-here

# ==============================================================================
# Runtime Configuration
# ==============================================================================
# Whether to use Docker for command execution
# Set to "False" to run commands directly on the host machine
USE_DOCKER=True

# ==============================================================================
# Docker User/Group Configuration (Linux/macOS)
# ==============================================================================
# Set these in the Dockerfile to match your host user
# Linux: typically 1000:1000
# macOS: typically 502:20
# Windows: typically 1001:1001
# USER_UID=1000
# USER_GID=1000
